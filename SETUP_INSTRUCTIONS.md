import { SettingsPage } from './pages/SettingsPage';

// Update your App.jsx to include the settings route:
// In the AppRoutes function inside the authenticated routes section, add:

{/*
<Route
  path="/settings"
  element={
    <AppLayout>
      <SettingsPage />
    </AppLayout>
  }
/>
*/}
